import apolloMiddleware from "./middleware";

/**
 * Module with functions that use instances of the @apollo/server module.
 */
const ApolloService = { apolloMiddleware };

export default ApolloService;
