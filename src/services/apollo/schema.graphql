enum RuleTypes {
  minSize
  minUppercase
  minLowercase
  minDigit
  minSpecialChars
  noRepeted
}

type Verify {
  verify: Boolean
  noMatch: [RuleTypes]
}

input Rule {
  rule: RuleTypes
  value: Int
}

type Query {
  verify(password: String!, rules: [Rule]): Verify
}
