enum RuleTypes {
  minSize
  minUppercase
  minLowercase
  minDigit
  minSpecialChars
  noRepeted
}

type Rule {
  rule: RuleTypes
  value: Number
}

type Verify {
  verify: Boolean
  noMatch: [RuleTypes]
}

type Query {
  verify(password: String!, rules: [Rule]): Verify
}
