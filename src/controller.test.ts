import { faker } from "@faker-js/faker";

import AppController from "./controller";

describe("Test App Controller", () => {
  it("minDigit", () => {});

  it("minLowercase", () => {});

  it("minSize", () => {});

  it("minSpecialChars", () => {});

  it("minUppercase", () => {});

  it("noRepeted", () => {});
});
